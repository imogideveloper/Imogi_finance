[project]
name = "imogi_finance"
authors = [
    { name = "PT. Inovasi Terbaik Bangsa", email = "m.abisena.putrawan@cao-group.co.id"},
    { name = "dannyaudian" },
    { name = "abisena", email = "m.abisena.putrawan@cao-group.co.id" },
]
description = "App for Manage Expense IMOGI"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    "PyMuPDF>=1.23.0",  # For PDF text extraction with coordinates
    "google-auth>=2.16.0",  # Google Cloud authentication
    "google-auth-oauthlib>=1.0.0",  # OAuth2 for Google APIs
    "requests>=2.28.0",  # HTTP library for API calls
]


# These dependencies are only installed when developer mode is enabled
[tool.bench.dev-dependencies]
# package_name = "~=1.1.0"

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0,<16.0.0"
erpnext = ">=15.0.0,<16.0.0"

[tool.ruff]
line-length = 110
target-version = "py310"

[tool.ruff.lint]
select = [
    "F",
    "E",
    "W",
    "I",
    "UP",
    "B",
    "RUF",
]
ignore = [
    "B017", # assertRaises(Exception) - should be more specific
    "B018", # useless expression, not assigned to anything
    "B023", # function doesn't bind loop variable - will have last iteration's value
    "B904", # raise inside except without from
    "E101", # indentation contains mixed spaces and tabs
    "E402", # module level import not at top of file
    "E501", # line too long
    "E741", # ambiguous variable name
    "F401", # "unused" imports
    "F403", # can't detect undefined names from * import
    "F405", # can't detect undefined names from * import
    "F722", # syntax error in forward type annotation
    "W191", # indentation contains tabs
]
typing-modules = ["frappe.types.DF"]

[tool.ruff.format]
quote-style = "double"
indent-style = "tab"
docstring-code-format = true
